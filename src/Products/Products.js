import React from 'react';
import Header from '../Header/Header';

const Products = () => {
    return (
        <div>
            <Header></Header>
            <h4>This is from products</h4>
        </div>
    );
};

export default Products;